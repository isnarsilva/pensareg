document.addEventListener("DOMContentLoaded",()=>{const header=PensaReg.select("[data-header]"),toggle=PensaReg.select("[data-menu-toggle]"),nav=PensaReg.select("[data-nav]"),links=PensaReg.selectAll('a[href^="#"]'),sections=PensaReg.selectAll("main section[id]");const closeMenu=()=>{toggle?.classList.remove("open");nav?.classList.remove("open");toggle?.setAttribute("aria-expanded","false");toggle?.setAttribute("aria-label","Abrir menu")};toggle?.addEventListener("click",()=>{const open=toggle.getAttribute("aria-expanded")==="true";toggle.classList.toggle("open",!open);nav.classList.toggle("open",!open);toggle.setAttribute("aria-expanded",String(!open));toggle.setAttribute("aria-label",open?"Abrir menu":"Fechar menu")});links.forEach(link=>link.addEventListener("click",closeMenu));document.addEventListener("keydown",event=>{if(event.key==="Escape")closeMenu()});const update=()=>{header?.classList.toggle("scrolled",scrollY>16);let current="";sections.forEach(section=>{if(scrollY>=section.offsetTop-150)current=section.id});PensaReg.selectAll(".main-nav a").forEach(link=>link.classList.toggle("active",link.getAttribute("href")==="#"+current))};addEventListener("scroll",update,{passive:true});update();const year=PensaReg.select("[data-current-year]");if(year)year.textContent=new Date().getFullYear()});